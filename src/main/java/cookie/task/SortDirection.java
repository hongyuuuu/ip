package cookie.task;

/** Identifies the direction in which comparable task values should be displayed. */
public enum SortDirection {
    /** Displays smaller values before larger values. */
    ASCENDING,
    /** Displays larger values before smaller values. */
    DESCENDING
}
